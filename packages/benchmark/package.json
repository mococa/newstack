{
  "name": "@newstack/benchmark",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node dist/server.js",
    "build": "esbuild --bundle --platform=node --format=cjs --external:esbuild esbuild.config.js | node",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@newstack/framework": "*"
  }
}
