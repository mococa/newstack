import Newstack from "@newstack/framework";

/**
 * Benchmark entrypoint for the Newstack Framework.
 */
export class Application extends Newstack {
  render() {
    return (
      <div>
        <h1>Newstack Framework Benchmark</h1>
        <p>This is a simple benchmark application using Newstack Framework.</p>
      </div>
    );
  }
}
